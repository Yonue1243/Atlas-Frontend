# Repo: web/ (Next.js). publish debe ser distinto de base (requerido por Netlify).

[build]
  base = "web"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
